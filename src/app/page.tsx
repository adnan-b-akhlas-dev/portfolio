import { Button } from "@/components/ui/button";

export default function Home() {
  return (
    <div>
      <Button>The quick fox jump over the lazy dog</Button>
      <Button className={"italic"}>The quick fox jump over the lazy dog</Button>
    </div>
  );
}
